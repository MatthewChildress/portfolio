import { ContactContainer } from './Contact.styled'

const Contact = (props) => {
    return(
        <ContactContainer id={props.id}>
            <h1>hi</h1>

        </ContactContainer>
    );
}

export default Contact;
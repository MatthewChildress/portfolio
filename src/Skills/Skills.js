import { SkillsContainer } from './Skills.styled'

const Skills = (props) => {
    return(
        <SkillsContainer id={props.id}>
            <h1>hi</h1>

        </SkillsContainer>
    );
}

export default Skills;